# 变更日志

## 未发布

- 保留原生属性状态 `props_status`，提交前阻断属性冲突，不改写内容状态（#14）。
- 修复 `status()` 的字面量 `@` 路径处理，保留字符串末尾空 peg 的兼容规则（#15）。
- 建立贡献、issue/PR、版本兼容、分支、标签及发布规范，增加 GitHub 模板和安全报告流程；CI 显式使用只读仓库权限，不改变运行时行为或包版本。
- 补充 issue 发现基线、修复起点与复测记录规则及填写例文，精简贡献指南和 PR 模板，统一文档表述。
- 明确普通提交不改包版本，发布准备时统一定版；源码问题通过完整提交 SHA 和未提交修改情况追踪。

## 0.2.3 - 2026-09-18

- 修复本地仓库错误码从路径文本误匹配的问题（#1）。
- 将显式提交目标的状态检查、自动添加和缺失清理限定于所选路径（#4）。
- 提交版本号仅接受完整的已知完成行；无法确认时返回 `None`，不查询猜测修订的历史（#5）。
- 为 `cat` / `cat_to_file` 增加可选 `max_output_bytes`，复用有界捕获、进程清理和原子替换；默认行为不变（#6）。
- 明确日志预算不限制原生 SVN 内存，以及 Windows 文件参数和 diff 标题的编码边界（#7、#8、#9）。
- 修复读取方法对末尾字面量 `@` 的 `Path` 处理；字符串保留空 peg 兼容规则，可用显式 `peg` 消歧（#10）。
- 精简首页、安装、使用和维护文档，删除跨项目说明与迁移历史，统一为技术参考表述。
- 补充文档独立性及真实本地 SVN 回归测试；运行时依赖和 Python 兼容基线不变。

## 0.2.2 - 2026-09-12

- 统一文本、二进制和流式文件执行入口的进程组隔离与进程树清理，限制超时后的管道等待。
- 提交及服务器端 copy/move 复用统一执行机制，保留 CommitResult 与超时异常契约。
- 新增五条执行路径的进程继承管道测试，覆盖超时返回、后代退出、原文件保留和半成品清理。
- 保持 `timeout=None` 默认值及 Python 兼容基线。

## 0.2.1 - 2026-09-08

- 完善安装、升级、离线部署和发布指南。
- 补充 SVNRepo 方法签名、返回模型和错误处理说明。
- 修正日志分页、跨平台路径和提交结果检查示例。
- 新增文档链接、示例语法和 API 签名一致性检查。

## 0.2.0 - 2026-09-07

- 首个独立发行版本，提供 SVN 命令封装、XML 解析器和结构化模型，支持 Python >=3.9。
- 增加类型标记 `py.typed`。
- 检出后核对目标 `.svn`；路径不符时抛出 SVNCommandError，保留客户端输出和错误目录。
- 记录 Windows ACP936 / TortoiseSVN 1.14.5 中文绝对路径兼容性限制。
- 配置 CI、真实本地 SVN 测试、发布标签保护、构建内容白名单与隔离导入检查。
