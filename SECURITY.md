# 安全政策

## 报告范围

本项目优先处理可能造成未授权操作、凭据泄露、错误目标写入、数据损坏或完整性校验绕过的问题。普通用法、性能和已知原生兼容限制仍可通过 issue 报告；无法判断是否安全问题时先使用私密渠道。

## 报告渠道

优先检查 [Security 页面](https://github.com/narutozb/pysvnlite/security)是否提供 “Report a vulnerability”。**只有管理员启用 GitHub 私密漏洞报告后，该入口才可用；本文件本身不会启用它。**

若没有私密入口，请在 issue 中仅请求“需要私密安全联系方式”，不要附漏洞细节、目标地址、凭据或可利用代码。管理员提供已确认的私密渠道后再发送材料，不猜测维护者邮箱，也不要将私有附件作为普通 issue 附件上传。

已暴露的 token 或密码应由拥有者立即撤销/轮换；删除评论不能使凭据重新安全。不得为证明漏洞而测试他人的系统或生产 SVN 仓库。

## 所需信息

- 受影响版本、安装来源、操作系统、Python/SVN 版本及配置条件。
- 潜在影响和调用边界，已知受影响及不受影响的版本。
- 使用合成数据和临时本地仓库的最小复现、脱敏证据。
- 临时缓解措施和报告者期望的署名方式（可匿名）。

维护者私下确认、定位和验证后协商披露时间，发布修复和必要公告。不承诺固定处理时限或漏洞奖励；安全处置不要求报告者公开秘密或提供生产访问权。

## 支持范围

优先为最新正式维护系列提供修复，其他系列按 [维护策略](docs/maintenance-policy.md)明确决定是否回补。欢迎报告旧版本的重大漏洞，不因版本较旧而要求公开利用细节。

测试通过不代表所有原生 SVN、SSH、GUI 或操作系统组合安全；原生客户端和 Python 的安全更新仍由其上游负责。不得以猜测路径/编码、扩大写入范围或绕过权限检查作为修复。

私密报告能力及未启用时的流程参考 [GitHub 安全报告说明](https://docs.github.com/en/code-security/how-tos/report-and-fix-vulnerabilities/report-privately)。
